<UserControl x:Class="QC_Management.Views.ExternalProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QC_Management.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             MinHeight="400" MinWidth="600">

    <!-- NOTE: DataContext removed so host (Window or parent) provides the VM instance -->
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel Margin="12">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBox Width="100"
                     Margin="0,0,8,0" 
                     Text="{Binding NewYear, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="260" Margin="0,0,8,0"
                     materialDesign:HintAssist.Hint="Tên chương trình"
                     Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="180" Margin="0,0,8,0" 
                     materialDesign:HintAssist.Hint="Nhà cung cấp"
                     Text="{Binding NewVendor, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Add" Width="80" Command="{Binding AddCommand}" />
            <Button Content="Save All" Width="90" Margin="8,0,0,0" Command="{Binding SaveCommand}" />
            <Button Content="Delete" Width="80" Margin="8,0,0,0" Command="{Binding DeleteCommand}" />
            <Button Content="Refresh" Width="80" Margin="8,0,0,0" Command="{Binding RefreshCommand}" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Programs}" SelectedItem="{Binding SelectedProgram}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="90"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor}" Width="200"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                <DataGridTextColumn Header="CreatedAt" Binding="{Binding CreatedAt}" Width="170" IsReadOnly="True"/>
                <DataGridTextColumn Header="CreatedBy" Binding="{Binding CreatedBy}" Width="140" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
