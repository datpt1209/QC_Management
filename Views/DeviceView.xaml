<UserControl x:Class="QC_Management.Views.DeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:QC_Management.Views"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"  
             TextElement.FontWeight="Regular" 
             TextElement.FontSize="13"  
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             DataContext="{StaticResource DeviceVM}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- three columns: devices | add/remove tests | rule configuration -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="1.6*"/>
            <ColumnDefinition Width="0.9*"/>
        </Grid.ColumnDefinitions>

        <!-- TOP: device header / input (unchanged, left column) -->
        <materialDesign:Card Grid.Row="0" Grid.Column="0" Margin="5">
            <WrapPanel Margin="0 0 0 10">
                <ComboBox 
                        Name="cbTest"
                        Margin="5"
                        ItemsSource="{Binding CategorytList}"
                        DisplayMemberPath="Name"
                        SelectedItem="{Binding SelectedCategory}"
                        materialDesign:ComboBoxAssist.MaxLength="2"
                        materialDesign:HintAssist.Hint="Category"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}"
                        IsEditable="True" 
                    VerticalAlignment="Center" Width="137" 
                    Height="50">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding CategorySelectionChangedCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ComboBox>

                <TextBox VerticalAlignment="Center"
                         materialDesign:HintAssist.Hint="Tên thiết bị"
                         Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         TextWrapping="Wrap" Height="50" Width="120" />
            </WrapPanel>
        </materialDesign:Card>

        <!-- TOP: control buttons (unchanged) -->
        <materialDesign:Card Grid.Row="1" Grid.Column="0" Margin="5">
            <WrapPanel>
                <Button Command="{Binding AddCommand}" Content="Thêm" Margin="5" Width="100"/>
                <Button Command="{Binding DeleteCommand}" Content="Xóa" Margin="5" Width="100"/>
                <Button Command="{Binding EditCommand}" Content="Cập nhật thiết bị" Margin="5" Width="140" CommandParameter="{Binding SelectedItem}"/>
            </WrapPanel>
        </materialDesign:Card>

        <!-- LEFT: device list (unchanged) -->
        <materialDesign:Card Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Stretch">
            <ListView ItemsSource="{Binding List}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding DeviceSelectionChangedCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" Width="40"/>
                        <GridViewColumn Header="Tên thiết bị" DisplayMemberBinding="{Binding Name}" Width="160"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </materialDesign:Card>

        <!-- MIDDLE: add/remove tests UI (kept as before) -->
        <materialDesign:Card Grid.Row="1" Grid.Column="1" Margin="5" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
            <TextBlock Text="Danh mục xét nghiệm thực hiện"/>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Grid.Column="1" Margin="5" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Available tests -->
                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel >
                        <TextBlock Text="Available tests" FontWeight="Bold" Margin="0,0,0,6"/>
                        <ListBox ItemsSource="{Binding TestList}" 
                                 SelectedItem="{Binding SelectedTest, Mode=TwoWay}"
                                 DisplayMemberPath="Name"
                                 HorizontalContentAlignment="Stretch"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 PreviewMouseWheel="InnerList_PreviewMouseWheel"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Center add/remove -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0">
                    <Button Command="{Binding AddTestCommand}" CommandParameter="{Binding SelectedTest}" Content="Add ▶" Margin="4" Width="100" />
                    <Button Command="{Binding RomoveCommand}" CommandParameter="{Binding SelectedDeviceTest}" Content="◀ Remove" Margin="4" Width="100" />
                </StackPanel>

                <!-- Assigned tests for selected device -->
                <ScrollViewer Grid.Column="2" Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel >
                        <TextBlock Text="Assigned to device" FontWeight="Bold" Margin="0,0,0,6"/>
                        <ListView ItemsSource="{Binding DeviceTestList}" SelectedItem="{Binding SelectedDeviceTest, Mode=TwoWay}" ScrollViewer.VerticalScrollBarVisibility="Auto" PreviewMouseWheel="InnerList_PreviewMouseWheel">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel LastChildFill="True">
                                        <TextBlock Text="{Binding IdTestNavigation.Name}" VerticalAlignment="Center" Margin="4,2"/>
                                    </DockPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </materialDesign:Card>

        <!-- RIGHT: rule configuration panel for the selected DeviceTest -->
        <materialDesign:Card Grid.Row="0" Grid.Column="2" Grid.RowSpan="3" Margin="5" Padding="10" VerticalAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Cấu hình rule Westgard" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Text="Chọn xét nghiệm ở giữa, rồi bật/tắt các rule bên dưới." FontStyle="Italic" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                
                <Border BorderBrush="#DDD" BorderThickness="1" Padding="6" CornerRadius="6" Margin="0,0,0,8" Background="White">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Height="420">
                        <ItemsControl ItemsSource="{Binding CurrentDeviceTestWestgardRuleItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Display}" 
                                              IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                              Margin="4"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Save rules by saving only DeviceTest.WestgardRulesJson -->
                    <Button Content="Lưu rule" Width="110" Margin="4" 
                            Command="{Binding SaveRulesCommand}"/>
                    <Button Content="Áp dụng cho tất cả" Width="150" Margin="4" 
                            Command="{Binding ApplyRulesToAllCommand}" />
                </StackPanel>

                <TextBlock Text="Lưu ý: thay đổi lưu khi nhấn 'Cập nhật thiết bị' hoặc 'Lưu rule'." FontSize="11" FontStyle="Italic" Margin="0,8,0,0"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>