<UserControl x:Class="QC_Management.Views.CustomersTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local1="clr-namespace:QC_Management.Models"
             xmlns:local="clr-namespace:QC_Management.Views"
             mc:Ignorable="d" 
             
             d:DesignHeight="300" d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance local:CustomersTooltip}">

    <Grid>
        <Border 
        Background="#E4F3F1F1"
        Padding="20 10" 
        BorderThickness="2" 
        BorderBrush="#555555"
        CornerRadius="10">
            <ItemsControl ItemsSource="{Binding DisplayPoints, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0">
                                <Run Text="LOT: "/>
                                <Run Text="{Binding Lot}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="1">
                                <Run Text="Date: "/>
                                <Run Text="{Binding DateRun, StringFormat=dd/MM/yyyy}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="2">
                                <Run Text="Time: "/>
                                <Run Text="{Binding Time, StringFormat=hh\\:mm\\:ss}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="3">
                                <Run Text="Kết quả: " FontWeight="Bold"/>
                                <Run Text="{Binding Result1}" FontWeight="Bold"/>
                            </TextBlock>
                            <TextBlock Grid.Row="4">
                                <Run Text="Lỗi: " FontWeight="Bold"/>
                                <Run Text="{Binding WestgardErrors}" FontWeight="Bold"/>
                            </TextBlock>

                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</UserControl>
